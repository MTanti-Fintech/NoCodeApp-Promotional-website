import { StaticSearchPipe } from './static-search.pipe';

describe('StaticSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
