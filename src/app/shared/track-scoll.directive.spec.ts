import { TrackScollDirective } from './track-scoll.directive';

describe('TrackScollDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackScollDirective();
    expect(directive).toBeTruthy();
  });
});
