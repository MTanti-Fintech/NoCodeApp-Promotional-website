import { HighlightOnFilterPipe } from './highlight-on-filter.pipe';

describe('HighlightOnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightOnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
